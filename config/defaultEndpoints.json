{
  "systemEndpoints": {
    "currentHost": "localhost",
    "hosts": [
      {
        "alias": "server",
        "endpoint": {
          "host": "localhost",
          "port": 9000
        }
      }
    ]
  }
}